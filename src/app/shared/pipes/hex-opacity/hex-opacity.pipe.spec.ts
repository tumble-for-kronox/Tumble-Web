import { HexOpacityPipe } from './hex-opacity.pipe';

describe('HexOpacityPipe', () => {
  it('create an instance', () => {
    const pipe = new HexOpacityPipe();
    expect(pipe).toBeTruthy();
  });
});
