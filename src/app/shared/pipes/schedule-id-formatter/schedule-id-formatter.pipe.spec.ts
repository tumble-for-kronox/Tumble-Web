import { ScheduleIdFormatterPipe } from './schedule-id-formatter.pipe';

describe('ScheduleIdFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleIdFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
